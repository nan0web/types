# Release v1.7.2 (NaN0 Parsing & Serialization Fixes)

## 🎯 Scope
Стабілізація формату NaN0 (`src/domain/NaN0.js`) для підтримки багатовимірних масивів, пустих вузлів (`{}` та `[]`) та складених об'єктів з ключами-масивами усередині батьківських масивів. Зміни, описані в `seed.md`, вже імплементовані. Наша ціль — зафіксувати їх контрактними тестами та упевнитись у 100% сумісності.

## 📋 План дій (Next Actions)

- [x] **Крок 1. Контрактні тести (TDD).** 
    Створити або оновити тести (`src/test/releases/...` або в `README.md.js`) для фіксації нової поведінки `NaN0`:
    - Коректна серіалізація `{}` та `[]` усередині масиву (уникнення `[object Object]`).
    - Успішний парсинг дефісу без пробілу (`-`) для масивів усередині масивів.
    - Парсинг складених структур (наприклад, елемент масиву, що містить як звичайні `key: value`, так і вкладені `children: []`), з коректним виходом фрейму парсера (pop) до батьківського об'єкту.

- [x] **Крок 2. Code Review `NaN0.js`.** 
    Перевірити поточну імплементацію `src/domain/NaN0.js`, яка вже містить виправлення. Упевнитись, що алгоритми серіалізації/парсингу покривають щойно описані контрактні тест-кейси без регресій.

- [x] **Крок 3. Регресійне тестування.** 
    Запустити `npm run test:all` в директорії `@nan0web/types`, щоб переконатись, що всі існуючі тести та нові контрактні проходять успішно. Якщо падають — доопрацювати парсер або тести.

- [x] **Крок 4. Завершення релізу.**
    Підтвердити 100% покриття тестів, перевірку чистоти гігієни (`knip`, `audit`) та підготувати підсумок для автозакриття.
